执行阶段 4：把快捷捕获与 Markdown 做成可靠的完整使用闭环。

Desktop agent 负责快捷键和 Capture 窗口生命周期；UI agent 负责输入、草稿状态、源码/预览；Data 所需修改在前两者契约需求明确后单独委派，避免同时改共享文件。你负责 preload/IPC 集成与跨窗口数据刷新。最后另请 QA 审核异常路径。

默认 Ctrl+Shift+Space 唤起 Capture，冲突时明确提示，可配置且保留可用入口。默认创建笔记，可显式选择任务/日程；不把时间文字或 Markdown 清单自动转换为独立任务。

Enter 换行，Ctrl+Enter 保存成功后收起，Esc 保留草稿；失焦不提交或清空。处理 compositionstart/compositionend/isComposing 及真实中文输入法候选确认。不能只凭模拟键盘事件声称中文输入法已验收。

实现去抖草稿持久化、保存状态、重启恢复、防乱序覆盖和重复提交防护。正式创建成功与草稿清理应具有明确事务/幂等策略。模拟磁盘写入失败：输入仍在、窗口不假成功关闭、可以重试。

Markdown 支持规定语法；默认禁用原始 HTML，不加载远程图片。外链仅经受限接口在协议/目标验证后打开；不能向 renderer 暴露任意 shell。长文本、代码块和表格在 Library 阅读，Widget 显示摘要。

验收覆盖：中英混排、多行、中文候选确认、Esc/失焦/重启、快速重复 Ctrl+Enter、故障重试、恶意 HTML/链接、另一个窗口实时看到变化。测试与真实 userData 隔离，未执行人工测试标 NOT_RUN。

交付可实测的捕获流程和结果，更新进度后停止。
