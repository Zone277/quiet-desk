执行阶段 5：Daily Log、按日期历史与删除传播。

Data agent 负责历史查询/日志生成和局部测试；UI agent 负责 Library 的日期浏览、生成区/手写区、回收站与 Markdown 导出界面；QA 负责独立跨天测试。三者不修改彼此的目录，公共契约由你处理。

日志包括当天完成、截至该日日界线仍待办、当日安排（明确标为计划）、当日笔记。当天可更新；历史基于当时操作快照，不能用当前状态重写。生成区与手写补充区分别持久化。生成项必须有实体关联与可移除的结构，避免永久删除后大段已生成 Markdown 仍残留正文。

使用可注入 Clock，固定日期与时区测试以下序列：
A 日创建/安排任务未完成；B 日完成；C 日首次打开 A 日日志。A 日应仍表达当时未完成，B 日记录完成。C 日改标题/改期/重新打开，不无意重写 A/B 日历史。
再验证同日完成后重新打开，遵循阶段 2 固定规则。

跨日时程序未运行也能在启动、唤醒或查看日期时补齐；多次生成幂等。测试时区边界，不改系统时间。无活动空白日期按需生成，不批量制造空日志。

测试任务、笔记、日程的删除→历史自动区隐藏→恢复→历史可重建→确认永久删除→相关正文/快照逻辑清理。用户独立手写内容不按关键词误删；外部导出/备份属于独立副本的边界在界面说明。

实现 UTF-8 Markdown 导出，使用原生保存对话框和受限导出接口；校验当次导出不含已隐藏的自动生成内容。正常链接、代码块与中英文不损坏。

验收必须包含真实 SQLite 数据及重启场景，不仅是纯函数示例。报告证据并停止。
