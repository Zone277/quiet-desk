执行阶段 3：基于已冻结契约，真实并行完成业务数据层和 Widget 界面。

先检查阶段 2 契约存在且无待决阻塞。给 Data 与 UI 分配互不重叠路径，QA 负责独立测试设计；最多 3 个子 agent。公共类型或依赖变更统一向你提出，不能各自修改。

Data agent：只修改 domain、主进程数据/服务与局部数据测试。实现任务/笔记/日程的创建编辑、完成/重新打开、改期、回收站/恢复/确认后永久删除；当前待办与未来日期查询；跨午夜/全天重叠查询；持久化草稿；为历史还原保存必要操作快照。变更、操作记录与事件发布采用正确事务边界，SQL 参数化。永久删除不遗留应用管理的关联正文。

UI agent：只修改 renderer 与局部组件测试。实现 Sonoma 风格 Widget、轻量日期浏览入口、Capture 和 Library 基础布局、设计 tokens、中文/英文、浅色/深色/跟随系统。支持 320×240、480×420、720×720 及非预设尺寸；最小尺寸也保留全部核心入口。完成当前任务后折叠到今日已完成，不让历史挤占首页。不使用 Apple 字体或复制其品牌素材。

UI 可在开发预览中使用明确标识的测试 fixtures，但最终产品路径必须接真实 preload API；不能以 mock、localStorage 或静态演示卡片宣称业务完成。UI 不得硬编码业务日期查询规则。

Lead 集成 IPC 与窗口更新通知，把 UI 接到真实数据后重新测试。至少证明：创建中文任务→重启仍在→完成→历史可查→恢复为待办；昨日任务不自动改期；跨午夜日程在两天均可见。

QA 检查空状态、大量条目、长中英混排、保存失败与小尺寸布局。具备渲染环境时保存并实际检查截图；只生成截图但未检查不能算视觉验收。

更新真实测试结果和文件变更，停止在当前阶段。
